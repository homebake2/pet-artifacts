---
id: f7eab76a-0c6e-4291-8ec6-b5a3830cf910
title: Удаление события — Frontend
parent_page: PET/pages/calendar/index.md
workitems: []
created_at: 2026-08-24
updated_at: 2026-09-03
---
## Смотрите также

* [Общие требования: Формат ошибок API](../common/obschie-trebovaniya-format-oshibok-api.md)

* [Общие требования: Soft-delete](../common/obschie-trebovaniya-soft-delete.md)

* [Удаление события — Frontend (dataSource=local)](udalenie-sobytiya-frontend-local.md) — детальное поведение этого экрана в режиме local (мягкое удаление действует одинаково в обоих источниках данных).

## Назначение

Флоу позволяет пользователю удалить событие прямо из списка на экране календаря жестом "смахнуть влево" с обязательным подтверждением в диалоге, а также через long-press меню как альтернативный способ (см. "Обработка ошибок").

## Действующие лица и предусловия

Авторизованный пользователь, просматривающий список событий выбранного дня; на экране отображена хотя бы одна карточка события.

## Основной сценарий

1. Пользователь смахивает карточку события влево (горизонтальный жест) либо вызывает long-press меню на карточке и выбирает пункт удаления — оба способа приводят к одному и тому же диалогу подтверждения.

2. Если смахивание достаточно значительное, карточка визуально смещается, открывая область удаления.

3. В этом случае система показывает диалог подтверждения с опциями "Отмена"/"Подтвердить". Если смахивание незначительное, карточка плавно возвращается в исходное положение без диалога.

4. При подтверждении система блокирует кнопку "Подтвердить" и показывает индикатор выполнения (см. "Бизнес-правила и валидация"), затем инициирует удаление события: онлайн — `DELETE /events/{id}` (мягкое удаление на сервере, см. [Общие требования: Soft-delete](../common/obschie-trebovaniya-soft-delete.md)); офлайн — удаление события из локальных данных питомца (pet.events).

5. Система дожидается результата операции удаления и только после успешного завершения убирает карточку из списка и закрывает диалог.

## Бизнес-правила и валидация

* Порог срабатывания диалога подтверждения определяется на уровне реализации интерфейса (конкретные пороговые значения смахивания не фиксируются в требованиях).

* Удаление события всегда требует явного подтверждения пользователя через диалог — прямого удаления без подтверждения не предусмотрено.

* Кнопка "Подтвердить" в диалоге блокируется, и на ней показывается индикатор выполнения на всё время запроса удаления — это защищает от повторной отправки запроса при двойном тапе.

* Права на удаление проверяются на сервере; клиент не выполняет собственную проверку прав перед отправкой запроса.

## Обработка ошибок

* Если пользователь выбирает "Отмена" в диалоге, карточка возвращается в исходное положение, запрос на удаление не отправляется.

* Если смахивание незначительное и не достигло порога срабатывания, диалог не показывается, и карточка возвращается в исходное положение без каких-либо дальнейших действий.

* 404 от `DELETE /events/{id}` трактуется как "событие уже удалено или недоступно" (см. [Общие требования: Soft-delete](../common/obschie-trebovaniya-soft-delete.md)) и обрабатывается как успешное удаление: система тихо убирает карточку из списка и инвалидирует кэш активностей — без показа ошибки пользователю, как будто удаление прошло успешно.

* Если операция удаления завершилась иной ошибкой (сеть, 400/500 — онлайн, либо ошибка записи в локальное хранилище — офлайн), система должна вернуть карточку в список в исходном (неудалённом) виде и показать пользователю понятное сообщение об ошибке — карточка не должна пропадать из UI до подтверждения фактического удаления на сервере/в хранилище.

## Хранение состояния и побочные эффекты

* Онлайн: после успешного 204 No Content (или трактуемого как успех 404, см. выше) система инвалидирует кэш активностей (['activities', petId, ...]) затронутого питомца, чтобы карточка не появилась повторно при следующем обновлении списка и чтобы индикаторы календаря отразили изменение.

* Офлайн: удаление сразу отражается в локальном хранилище питомца.

* Удаление на сервере — мягкое (устанавливается deleted_at, строка не удаляется физически); политика хранения мягко удалённых событий и восстановления описаны на странице "Удаление события — Backend" и в [Общие требования: Soft-delete](../common/obschie-trebovaniya-soft-delete.md).
