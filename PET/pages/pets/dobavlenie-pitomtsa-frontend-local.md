---
id: 2481f019-6365-4dc3-a58d-ef110921cc2d
title: Добавление питомца — Frontend (dataSource=local)
parent_page: PET/pages/pets/index.md
workitems: []
created_at: 2026-09-03
updated_at: 2026-09-04
---
## Смотрите также

* [Добавление питомца — Frontend](dobavlenie-pitomtsa-frontend.md) — общий сценарий экрана; эта страница описывает только ветку `dataSource=local`.

* [Фотография питомца — Frontend (dataSource=local)](fotografiya-pitomtsa-frontend-local.md) — сохранение выбранной на форме фотографии (`photoUri`).

* [Локальный режим (dataSource=local) — общая архитектура хранения](../common/lokalnyi-rezhim-datasource-local-zaglushka.md) — модель хранения питомцев.

## Назначение

Определяет, как система сохраняет нового питомца, когда активен режим `dataSource=local`.

## Алгоритм сохранения

1. Клиентская валидация формы выполняется тем же набором правил, что описан на странице «Добавление питомца — Frontend», независимо от источника данных.

2. Клиент генерирует новый `id` питомца (UUID v4) и формирует запись питомца с полями формы плюс `id`, `ownerId` (id текущего аккаунта) и `isDeleted: false`. Если на форме была выбрана фотография, она копируется в постоянное хранилище приложения и её путь записывается в поле `photoUri` этой же записи (см. «Фотография питомца — Frontend (dataSource=local)») — в отличие от сетевого режима, здесь это не требует отдельного шага после создания питомца, так как нет сервера, ожидающего готового id. Идентификатор известен уже в момент сохранения — переход на карточку созданного питомца не зависит от асинхронного ответа сервера.

3. Кнопка «Сохранить»/«Создать» переходит в состояние загрузки и блокируется на время записи (см. [Общие требования: UI-паттерны запросов](../common/obschie-trebovaniya-ui-patterny-zaprosov.md)).

4. Клиент записывает новую запись в локальную коллекцию питомцев и дожидается подтверждения записи, после чего показывает результат и переходит на карточку созданного питомца.

## Обработка ошибок

Ошибка записи в локальное хранилище показывается пользователю тем же способом, что и сбой сохранения в сетевом режиме (см. [Общий раздел «Обработка ошибок хранилища»](../common/lokalnyi-rezhim-datasource-local-zaglushka.md)); пользователь остаётся на экране с введёнными данными формы.
