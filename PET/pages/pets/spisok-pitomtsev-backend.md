---
id: ca9f2d10-de10-4bcd-8b06-eaa9c1186a39
title: Список питомцев — Backend
parent_page: PET/pages/pets/index.md
workitems: []
created_at: 2026-08-24
updated_at: 2026-09-03
---
**Смотрите также:**

* [Общие требования: IDOR и владение ресурсами](../common/obschie-trebovaniya-idor-i-vladenie-resursami.md)

* [Общие требования: Soft-delete](../common/obschie-trebovaniya-soft-delete.md)

* [Общие требования: Формат ошибок API](../common/obschie-trebovaniya-format-oshibok-api.md)

* [Справочник значений (словари enum)](../common/spravochnik-znachenii-slovari-enum.md)

## Назначение

Эндпоинт `GET /pet` должен возвращать краткую информацию обо всех питомцах, принадлежащих текущему авторизованному пользователю, для главного экрана «Питомцы». Пагинация, фильтрация и сортировка (кроме фиксированного порядка, см. ниже) не входят в объём текущей итерации.

## Действующие лица и предусловия

* Инициатор — авторизованный пользователь.

* Требуется валидный access-токен (заголовок `Authorization: Bearer <access_token>`).

* Наличие строки в таблице `profile` не требуется — доступ к питомцам не зависит от заполненности профиля.

## Основной сценарий

`GET /pet`

1. Система извлекает `userID` из заголовка `Authorization` и проверяет валидность токена.

2. Система выбирает все строки таблицы `pet`, где `user_id = userID` и `deleted_at IS NULL` (мягко удалённые питомцы исключаются), отсортированные по `created_at` (`ORDER BY created_at`), чтобы порядок был стабильным между запросами.

3. Система возвращает `200 OK` с телом `{"items": [{"id","name","breed","species","icon"}]}`. Если у пользователя нет питомцев — `{"items": []}` со статусом `200`.

Заголовок `LanguageCode` не требуется этим эндпоинтом (см. правило ниже).

## Бизнес-правила и валидация

* Пользователь видит только своих питомцев.

* Мягко удалённые питомцы (`deleted_at IS NOT NULL`) всегда исключаются из списка — это часть единой политики скрытия мягко удалённых питомцев, действующей на всех эндпоинтах модуля (см. «Общие требования: Soft-delete»).

* Результат сортируется по `created_at` (`ORDER BY created_at`) — без явного порядка выборка была бы нестабильной между запросами.

* Заголовок `LanguageCode` не является обязательным параметром запроса: он не используется сервером для локализации содержимого ответа (сервер не хранит переводов), поэтому требование его обязательности снято на всех эндпоинтах питомцев для единообразия. Если это значение передано клиентом — оно игнорируется.

* Query-параметры фильтрации, поиска и сортировки не поддерживаются в текущей итерации.

* `species` хранится как свободный текст, а не как перечисление на уровне БД; `icon` — отдельное поле категоризации (набор из 30 допустимых значений), не обязанное дословно совпадать с текстом `species`. Оба поля используются одновременно и намеренно не объединяются. Актуальный список значений species, реально используемых фронтендом, зафиксирован на странице «Справочник значений (словари enum)»; сопоставление species/icon → иконка и дефолтная (fallback) иконка для значений вне справочника — ответственность фронтенда (см. «Список питомцев — Frontend»).

* Лимит на количество питомцев у одного пользователя не устанавливается в текущей итерации.

* Пагинация по питомцам отсутствует в текущей итерации — зафиксировано как осознанный технический долг MVP, отдельная задача на реализацию сейчас не заводится.

## Обработка ошибок

| Статус | Условие                                                                                                                      |
| ------ | ---------------------------------------------------------------------------------------------------------------------------- |
| 401    | Заголовок `Authorization` отсутствует, некорректен или токен истёк.                                                          |
| 500    | Произошла ошибка при выборке питомцев (БД и т.п.). Формат тела ошибки — см. «Общие требования: Формат ошибок API».           |

## Хранение состояния и побочные эффекты

Операция только читает данные (`GET`) и не изменяет состояние БД.
