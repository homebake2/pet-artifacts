---
id: 74657f7b-3a10-4f31-843d-baa86ae81858
title: Перенос локального аккаунта в сеть — Frontend
parent_page: PET/pages/profile/index.md
workitems: []
created_at: 2026-09-03
updated_at: 2026-09-03
---
## Смотрите также

* [Локальный режим (dataSource=local) — общая архитектура хранения](../common/lokalnyi-rezhim-datasource-local-zaglushka.md) — модель локальных коллекций, из которых формируется перенос, и правило «переключение источника данных не переносит и не объединяет данные автоматически», которое эта страница уточняет применительно к явному пользовательскому переносу.

* [Импорт локальных данных — Backend](import-lokalnykh-dannykh-backend.md) — контракт эндпоинта `POST /import/local-data`, которым завершается перенос.

* [Вход и регистрация — Frontend](../auth/vhod-i-registratsiya-frontend.md), [Вход и регистрация — Backend](../auth/vhod-i-registratsiya-backend.md) — форма и эндпоинт, которыми создаётся новый сетевой аккаунт перед переносом.

* [Импорт из локального аккаунта — Frontend](import-iz-lokalnogo-akkaunta-frontend.md) — обратный по направлению инициатора сценарий (импорт в уже существующий сетевой аккаунт), использующий тот же backend-контракт.

* [Общие требования: UI-паттерны запросов](../common/obschie-trebovaniya-ui-patterny-zaprosov.md), [Общие требования: Формат ошибок API](../common/obschie-trebovaniya-format-oshibok-api.md).

## 1. Назначение

Кнопка «Перенести аккаунт в сеть» на экране просмотра профиля в режиме `dataSource=local` позволяет пользователю единоразово перенести данные текущего локального аккаунта (профиль, питомцы, события) на сервер, создав для них новый сетевой аккаунт, и продолжить работу в режиме `network` под этим аккаунтом.

Перенос — однонаправленная (local → network) операция. Она не является тем же самым, что переключение `dataSource`: обычное переключение источника данных (см. «Локальный режим (dataSource=local)», раздел «Переключение источника данных») никогда не переносит и не объединяет данные — перенос, описанный на этой странице, наоборот, ЕСТЬ явное действие пользователя, специально предназначенное для переноса, и выполняется отдельной кнопкой, а не автоматически при переключении переключателя.

## 2. Действующие лица и предусловия

* Инициатор — пользователь, находящийся в режиме `dataSource=local`, на экране просмотра профиля (см. «Просмотр профиля — Frontend (dataSource=local)»).

* Кнопка «Перенести аккаунт в сеть» видна только в режиме `local`; в режиме `network` она отсутствует (для аналогичного действия в обратную сторону см. «Импорт из локального аккаунта — Frontend»).

* Активный локальный аккаунт (гостевой или логин/пароль) — источник переноса. Точный набор данных — профиль (если есть запись в локальной коллекции «Профили»), все не мягко-удалённые питомцы и события этого аккаунта.

## 3. Основной сценарий

1. Пользователь нажимает «Перенести аккаунт в сеть» на экране профиля.

2. Клиент показывает диалог подтверждения (см. раздел 4, «Подтверждение») с предупреждением, что будет создан новый сетевой аккаунт, и текущие локальные данные будут в него скопированы, а сами локальные данные при этом останутся на устройстве без изменений (см. раздел 6). Пользователь может отменить — тогда сценарий завершается без побочных эффектов.

3. После подтверждения клиент показывает форму создания сетевых учётных данных — переиспользует ту же форму «логин + пароль», что и обычный экран «Вход и регистрация» (см. «Вход и регистрация — Frontend»), с тем же клиентским контрактом валидации (`login` ≥ 2 символов, `password` ≥ 8 символов) и тем же эндпоинтом `POST /auth/login`/`POST /auth/register` для создания аккаунта. Локальный логин/пароль текущего локального аккаунта НЕ переиспользуются автоматически ни в каком виде (включая гостевые аккаунты, у которых пароль не задавался пользователем осмысленно) — пользователь ОБЯЗАН задать новые сетевые учётные данные явно, проходя тот же путь, что обычная регистрация.

4. Клиент отправляет запрос создания аккаунта (см. «Вход и регистрация — Backend»). При ошибке — см. раздел 5, перенос не начинается.

5. При успешном ответе клиент сохраняет полученную пару токенов (как при обычном входе, см. «Вход и регистрация — Frontend», раздел «Хранение состояния») — с этого момента новый сетевой аккаунт существует и авторизован, но данные в него ещё не перенесены.

6. Клиент формирует тело запроса `POST /import/local-data` (см. «Импорт локальных данных — Backend»):
   * `pets` — все питомцы текущего локального аккаунта с `deletedAt == null`, с полями, соответствующими форме питомца, и `local_id`, равным их локальному `id`;
   * `events` — все события текущего локального аккаунта с `deletedAt == null` (событие мягко удалённого питомца при этом уже исключено, так как соответствующий питомец не мягко удалён — см. «Локальный режим (dataSource=local)», раздел «Мягкое удаление», каскадное удаление событий), с `pet_local_id`, равным локальному `id` их питомца;
   * `profile` — запись из локальной коллекции «Профили» для текущего аккаунта, если она существует, иначе `null`.

7. Клиент генерирует `Idempotency-Key` (UUID v4) один раз на попытку переноса — до первой отправки запроса — и переиспользует его при повторной отправке после сетевой ошибки/таймаута (тот же принцип, что у Idempotency-Key при создании питомца, см. «Добавление питомца — Frontend»).

8. Кнопка подтверждения перехода в состояние загрузки на всё время выполнения переноса (создание аккаунта + перенос данных воспринимаются пользователем как единое действие, поэтому индикатор загрузки не убирается между шагами 4 и 9).

9. При успешном ответе `POST /import/local-data` клиент записывает текущую дату/время в поле `transferredAt` записи перенесённого аккаунта в локальной коллекции «Аккаунты» (см. «Локальный режим (dataSource=local)», раздел «Переключение источника данных»), переключает `dataSource` на `network` (без отдельного предупреждающего диалога переключения — пользователь только что осознанно запросил именно это действие) и переходит на главный экран сетевого режима под новым аккаунтом.

## 4. Бизнес-правила и валидация

* **Подтверждение.** Перенос запускается только после явного подтверждения в диалоге (шаг 2) — необратимость в бытовом смысле (сетевой аккаунт и перенесённые данные не удаляются автоматически кнопкой «Отмена» уже после переноса) требует того же паттерна предупреждения, что у переключения `dataSource`.

* **Локальные данные не удаляются.** Ни при успешном, ни при неудачном переносе клиент не удаляет и не изменяет исходные локальные данные — они продолжают существовать как независимый набор данных режима `local` (см. «Локальный режим (dataSource=local)», раздел «Переключение источника данных»). Пользователь, при желании удалить их, использует существующий отдельный путь удаления локального аккаунта (см. настройки локального режима) — эта страница его не переопределяет.

* **Один перенос — один новый сетевой аккаунт.** Сценарий не предполагает выбора существующего сетевого аккаунта в качестве цели — для этого предназначен обратный сценарий «Импорт из локального аккаунта», где пользователь уже авторизован в сети.

* **Пустой локальный аккаунт.** Если у локального аккаунта нет ни одного питомца/события и нет профиля — перенос всё равно допустим (создаётся аккаунт без данных); отдельного блокирующего предупреждения «нечего переносить» не требуется, так как создание аккаунта — самостоятельно ценное действие.

* **Повторный перенос уже перенесённого аккаунта.** Если у текущего локального аккаунта уже заполнено поле `transferredAt` (см. «Локальный режим (dataSource=local)», раздел «Переключение источника данных») — то есть этот аккаунт (или его часть данных) уже был перенесён или импортирован ранее, — диалог подтверждения (шаг 2) ОБЯЗАН дополнительно показать предупреждение о том, что перенос этого аккаунта уже выполнялся и повторный перенос может задублировать данные на сервере. Это предупреждение не блокирует продолжение сценария — пользователь по-прежнему может подтвердить перенос, отдельно осознав риск дублирования.

## 5. Обработка ошибок

| Условие | Что делает клиент | Что видит пользователь |
| --- | --- | --- |
| Ошибка создания сетевого аккаунта (400 VALIDATION_ERROR, 403 FORBIDDEN, сетевая ошибка/500) | Перенос данных не начинается; ошибка обрабатывается тем же способом, что на обычном экране входа (см. «Вход и регистрация — Frontend», раздел «Обработка ошибок») | То же сообщение об ошибке, что на экране входа/регистрации, на форме создания учётных данных |
| `POST /import/local-data` завершился ошибкой (см. «Импорт локальных данных — Backend», раздел 5) | Клиент показывает явную ошибку переноса; сетевой аккаунт при этом уже создан и токены сохранены — клиент НЕ откатывает создание аккаунта и НЕ переключает `dataSource` на `network`, оставляя пользователя в режиме `local` с уже существующим (но пустым) сетевым аккаунтом | Сообщение «Не удалось перенести данные» с кнопкой «Повторить», переиспользующей тот же `Idempotency-Key` (см. шаг 7) — повтор безопасен благодаря дедупликации на бэкенде |
| Пользователь отменяет перенос в диалоге подтверждения (шаг 2) | Сценарий завершается, форма создания аккаунта не показывается | Диалог закрывается, пользователь остаётся на экране профиля |
| 401 при вызове `POST /import/local-data` (маловероятно сразу после выдачи токена, но теоретически возможно при гонке) | Обрабатывается общим правилом авто-refresh (см. «Общие требования: Сессия, 401 и авто-refresh токена»); после успешного refresh запрос переноса повторяется автоматически | Индикатор загрузки продолжает отображаться до результата повтора |

Частичный перенос (часть питомцев/событий создана, часть — нет) невозможен по контракту backend (см. «Импорт локальных данных — Backend», атомарность) — ошибка `POST /import/local-data` всегда означает, что НИ ОДНА сущность не перенесена, и повтор с тем же `Idempotency-Key` безопасен.

## 6. Хранение состояния и побочные эффекты

* Успешный перенос создаёт токены нового сетевого аккаунта в защищённом хранилище (см. «Вход и регистрация — Frontend», раздел «Хранение состояния») и переключает активный `dataSource` приложения на `network`.

* Исходные локальные данные (профиль, питомцы, события) остаются без изменений на устройстве — перенос их не читает деструктивно. Единственное изменение, которое перенос вносит в саму запись локального аккаунта, — поле `transferredAt` (см. шаг 9); оно не влияет на видимость или содержимое данных аккаунта, а только фиксирует факт переноса для последующего предупреждения (см. раздел 4, «Повторный перенос уже перенесённого аккаунта»). Повторный перенос того же локального аккаунта — если пользователь вернётся в режим `local` — по-прежнему создаст ещё один независимый сетевой аккаунт с ещё одной копией данных; явного запрета не вводится, только предупреждение.

* Кэш `GET /profile` (react-query) на новом сетевом аккаунте не может быть «устаревшим», так как заполняется впервые в рамках этого же сценария; отдельная инвалидация не требуется.
